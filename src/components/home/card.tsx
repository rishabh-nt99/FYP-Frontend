import React from "react";

interface CardProps {
  title: any;
  desc1: string;
  desc2?: string;
  link: string;
}

const Card: React.FC<CardProps> = ({ title, desc1, desc2, link }) => {
  return <></>;
};

export default Card;
